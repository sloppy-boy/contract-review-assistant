# contract-review-assistant 代码审查报告

- **审查时间**：2026-08-16
- **执行模型**：kimi-k3（经本地代理 → opencode.ai/zen/go）
- **审查范围**：`K:\dph\contract-review-assistant` 全量初始实现
  - 基线：`review/full-audit`（ecb881a，空基提交）
  - 目标：`main`（ab07f1c "chore: initial commit"）
  - 差异：`git diff review/full-audit..main`，196 文件 / 11,805 行新增
- **方法**：双轴并行审查（标准轴 + 规范轴），两个子代理独立运行后聚合
  - 标准轴：对照 README/TESTING 自述约定 + Python/Vue 通用最佳实践
  - 规范轴：对照 README.md / docs/TESTING.md / test-contracts/README.md 声明的功能
- **审查 skill**：`review`（HEAD↔固定点差异审查流程）

---

## 结论摘要

| 轴 | 硬违规 | 判断调用 | 其他 |
|---|---|---|---|
| **Standards（标准）** | 4 | 13 | — |
| **Spec（规范）** | — | — | 缺失/部分 2、范围蔓延 2、实现与规范不符 2 |

**最严重问题（优先修复）**：
1. **[规范] 复核模型路由未接通** —— 声明的 deepseek-reasoner 复核实际未生效，功能正确性 + 成本口径双重问题
2. **[标准] api.py:57 头注入 / 中文文件名 500** —— 安全 + 稳定性硬伤

README/TESTING 自述约定（13 类 worker、三档消融、8K 预算、三档法条、18 单测、评分口径）经核对与代码一致，未见偏离。

---

## Standards（标准轴）

### 硬违规

| # | 位置 | 问题 | 依据 |
|---|---|---|---|
| S1 | `app/api.py:57` | `Content-Disposition: filename="{name}.docx"` 直接拼接用户提交的 `report.contract.name`，未消毒：含 `"` 破坏头格式（头注入）；含中文触发 latin-1 编码异常 → 导出 500 | 通用安全实践（用户输入不得未消毒入 HTTP 头） |
| S2 | `app/api.py:24,33-39` | 进程级 `TASKS` dict 多线程读写无锁；`_trim_tasks` 遍历 `TASKS.items()` 时另一线程 `TASKS[task_id]=…` 可抛 "dictionary changed size during iteration" | 并发/状态管理最佳实践；与本文件 docstring"并发隔离（必须）"自相矛盾 |
| S3 | `app/graph.py:81,118-121` | `_no_key()` 只看 `DEEPSEEK_API_KEY`，不看 `DSH_FORCE_MOCK`（nodes 层用 `using_mock()`）。有 key 时跑 smoke_test 仍建真实 client 并往 meta 写 `tokens=0/costYuan=0.0`，污染"成本按实际 usage"口径 | README"mock 数字无效/绝不产出可汇报数字"、TESTING"强制 mock 不烧 token" |
| S4 | `app/nodes/extract.py:98` | 兜底 clauseId 用 `abs(hash(chunk)) % 100000`：str hash 按进程加盐，跨运行不稳定 | README/TESTING"数据集可复现（两次生成 hash 一致）"的可复现约定 |

### 判断调用

| # | 位置 | 问题 |
|---|---|---|
| S5 | `app/llm.py:115` | `except Exception` 一律视为可重试（含 KeyError 等编程错误），重试后抛 LLMError 掩盖根因 |
| S6 | `app/api.py:79` | 上传无大小限制，全文直入 LLM 流水线；服务端无成本护栏，README"<1 元/份"仅靠自觉 |
| S7 | `frontend/src/api.js:38` | 轮询 `for(;;)` 无超时/上限，后端卡死则前端死循环 |
| S8 | `app/nodes/report.py:52` | `risk_clause_ids` 后者覆盖前者，同条款多 findings 时 riskLevel 未取最高严重度 |
| S9 | `scripts/test_api.py / export_demo.py / verify_manual.py / check_report_schema.py / analyze_missed.py / check_coverage.py / inventory.py` | 用 cwd 相对 `Path("eval/…")`，与 `config.ROOT` 绝对路径约定不一致，非仓库根运行即崩 |
| S10 | `scripts/export_eval_summary.py:36-37` | 跨模块调私有 `sc._cost/_latency` |
| S11 | `eval/blind_label.py:50` | 无重试，单次 HTTP 失败即中断整批盲标 |
| S12 | `requirements.txt:4` | 声明 `openai` 依赖但全库用 httpx 直连，未使用 |
| S13 | `app/nodes/extract.py` 与 `app/legal/rule_checker.py` | `_PCT_RE/_DAYS_RE/_AMOUNT_RE` 三处重复定义 |
| S14 | `eval/score.py:90-92` | `hit_fields` 用 `best_s >= 1-w+eps` 反推字段命中，0.7 分（条款+类型命中）时 severity 命中不被计数，诊断口径偏差 |
| S15 | `frontend/src/views/ReportDetail.vue:209-214` | `textContent.includes(clauseId)` 匹配导航项，短条款号（如"3"）易误中 |
| S16 | `app/graph.py:82` | 函数中部 `import time as _time`（PEP8 小疵） |
| S17 | `app/api.py:52` | 500 detail 直接回显 `str(e)`（内部信息泄露，轻微） |

---

## Spec（规范轴）

### 已核对且符合

LangGraph 流水线 + 黑板 + findings reducer 去重（README L30）、13 类 worker 并行扇出与风险矩阵唯一锚（L23）、≤8K 预算 / top-3 法条 / ≤3 句引用摘要（L31）、A/B/C 消融 + 打回重证一次迭代 + 高危 none 档查证（L32）、三档法条 + 禁旧合同法（L10）、严格三元组计分 + 部分分 + 三组口径 + 边界组 + 及格线 ✅/❌（L62-84，eval/score.py）、规则基线朴素实现（rule_checker.py）、dev43/test19/demo3 + meta.manualAudit 17.7%（TESTING 4.1）、盲标 Qwen（eval/blind_label.py）、FastAPI /upload /report /health /export/word、Vue 三视图 + 离线缓存真实导出、Word 导出、18 个单测数量吻合。

### 缺失/部分实现

| # | 功能点 | 现状 | 规范出处 |
|---|---|---|---|
| P1 | 前端"复制文本粘贴"输入 | Workbench.vue 仅有文件上传，无粘贴框（reviewText 提示语自称支持粘贴但无入口） | test-contracts/README.md L36-37"拖拽上传（或复制文本粘贴）" |
| P2 | "schema 校验降级 ✅（单测验证）" | tests/test_core.py 18 用例只覆盖 reducer/抽取/基线/计分/预算，无任何 schema 重试/降级测试（实现逻辑存在于 workers/reviewer，但"单测验证"声明不实） | TESTING.md 三 L37 |

### 范围蔓延

| # | 内容 | 问题 |
|---|---|---|
| R1 | `scripts/analyze_missed.py`、`check_coverage.py`、`smoke_real.py` | 未列入资产清单（清单仅 10 个）—— TESTING.md 六 L147 |
| R2 | `scripts/_test_export.docx` | 临时导出误提交的残留产物，无任何规范出处 |

### 实现与规范不符

| # | 位置 | 问题 | 对照 |
|---|---|---|---|
| M1 | `config.py:40-41` / `api.py /health` / `llm.py reviewer()` / `graph.py` | **复核模型路由未接通**：config 声明"复核（thinking 档）→ deepseek-reasoner"并配独立单价，api 上报 reviewerModel，llm.py 提供 reviewer()；但 graph.py 全程复用同一 deepseek-chat 客户端给复核节点，reviewer() 为死代码，复核实际未走 thinking 档、成本按 chat 单价计 | README L135"配置（模型名配置化）"、L97 复核模块级成本口径 |
| M2 | `frontend` 上传入口 / `api.py` | 上传 accept 声明 .doc/.docx/.pdf，但前端提示"支持 .txt"；后端 api.py 以 UTF-8 忽略错误解码，二进制格式上传必乱码，入口 accept 与实际能力不符 | test-contracts/README.md L4 仅承诺文本上传 |

---

## 建议修复优先级

**P0（功能正确性/安全，先修）**
- M1 复核模型路由接通（死代码 reviewer() → graph.py 接线 + 成本口径）
- S1 api.py:57 文件名消毒（头注入 / 中文 500）
- S3 `_no_key()` 纳入 `DSH_FORCE_MOCK`（mock 口径污染）

**P1（稳定性/一致性）**
- S2 TASKS dict 加锁或改线程安全结构
- S4 clauseId 兜底改稳定哈希（如 hashlib）
- S9 scripts 统一用 `config.ROOT` 绝对路径
- P2 补 schema 降级的单测（或修正 TESTING 声明）
- M2 统一上传类型声明与实际能力

**P2（干净化/可维护）**
- P1 前端补粘贴输入（或改提示语）
- R2 删 `_test_export.docx` 残留
- S12 清理未用 `openai` 依赖、S13 提取重复正则
- S5/S6/S7/S8/S10/S11/S14/S15 视精力逐项处理

> 说明：本报告为 kimi-k3 双轴并行审查的输出聚合，两轴故意分开、未做发现去重与合并排序，以便独立判断。修复后可按需重跑。
