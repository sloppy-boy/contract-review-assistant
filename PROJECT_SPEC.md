# 合同审查助手 · 项目规格书（PROJECT SPEC）

> 多 agent 协同的合同审查系统：上传采购/销售合同 → 自动输出「风险条款清单 + 修改建议 + 法条依据」。
> 定位：求职简历项目，需体现**生产价值**——有评测、有人工闭环、有可观测性、数字真实。
> 本文件是**唯一权威规格**，所有已锁定的技术决策均来自评审访谈，实现时不得擅自改动核心决策；如需调整，须先说明理由。

---

## 1. 项目概述

把一份采购/销售合同喂进去，系统用多 agent 流水线自动审查，输出：
- 🔴🟡🟢 分级的风险条款清单
- 每条风险：条款位置 + 原文摘录 + 风险类型 + 严重度 + **可溯源的法条依据** + **可直接替换的示范条款**
- 支持导出 Word / JSON，浏览器 Web UI 演示

与"玩具 demo"的分水岭（必须全部具备）：
1. **评测**：金标准集 + 严格计分口径 + 及格线，所有数字真实跑出
2. **对抗复核**：复核 agent 过滤误报，且其价值经**消融实验**验证
3. **法条可溯源**：三档法条依据，无依据不硬编，禁止虚构法条
4. **规则基线对比**：多 agent 系统必须"打赢"确定性规则基线，证明协同价值

---

## 2. 已锁定的核心决策

### 2.1 领域聚焦
- **合同类型：采购/销售合同**（企业最高频、条款最丰富、公开模板最多）
- **法条范围：精选"买卖法条手册"**（约 60~80 条），**不用全量语料**
  - 民法典合同编 · 买卖合同：**第 595~647 条**
  - 民法典合同编 · 违约责任：第 577~594 条；违约金：**第 585 条**；定金：**第 586 条**
  - 民事诉讼法：管辖条款（**第 35 条，现行 2021/2023 修正版编号**；2017 版旧编号第 34 条禁止使用）
  - 反不正当竞争法：商业秘密条款
- **版本硬约束**：2021 年民法典已取代 1999 年合同法，**禁止引用旧合同法**；每条法条带 `{法条ID, 版本, 条文号, 原文}`

### 2.2 技术栈
| 层 | 选型 | 备注 |
|---|---|---|
| 编排 | **LangGraph（Python 3.11+）** | 状态持久化、并行扇出、可观测 |
| LLM 主链路 | **DeepSeek API（v4-flash）** | 便宜；复核用 thinking 档 |
| 盲标第二模型 | **硅基流动 · Qwen3.5-122B-A10B**（备选 35B-A3B / 免费 Nex-N2-Pro） | 与 DeepSeek 不同家族，防共偏；**勿选硅基流动上托管的 DeepSeek 系列**（同家族无共偏价值），**勿选 8B 弱模型**（盲标会漏） |
| 后端 | **FastAPI** | `/upload` `/report` `/health` |
| 前端 | **Vue 3 + Element Plus（Vite）** | 单页三视图 |
| 评测 | `score.py` CLI | 计分 + 及格线 + 消融 + 规则基线 + 盲标质检 |

### 2.3 流水线架构（LangGraph 图）
```
上传合同
  → ① 条款抽取 agent
  → ② 风险识别 workers（每类一个，并行扇出）
  → ③ 复核 agent ──(驳回误报)──→ 打回原 worker 重证（一次迭代）
  → ④ 报告生成 agent
```

**① 条款抽取**
- 长文档**分块 / Map-Reduce**，不整篇塞上下文；扫描件前置 **OCR（PaddleOCR 或等效开源方案，模型文件入 gitignore）**
- 保留**条款编号 + 原文位置**（报告要能溯源到具体条款/款）
- 输出 `crossReferences`：条款间引用映射（"第 X 条 → 引用第 Y 条"）
- 异常条款（附件、表格、"此处空白"）兜底，不使整条链路崩溃
- **全局关联步骤（必须）**：分块抽取后**合并、再通读一次**生成全合同级的 definitions 与 crossReferences——分块会切碎"定义在第 1 页、引用在第 10 页"的跨页关联，不做全局关联则黑板事实残缺

**② 风险识别 workers（并行扇出）**
- 分类至少覆盖：付款/开票、验收交付、知识产权、数据合规、违约责任、解除权、保密、竞业限制、管辖/仲裁、通知送达、不可抗力、税费、外包分包；**与 2.6 风险矩阵一一对应（风险矩阵为分类唯一来源），不得自行增删分类**
- **每个 worker 只收到：职责相关条款 + 被这些条款引用的其他条款摘要（2~3 句）**——防"看单条下结论"，这是交叉引用盲区的解法
- **输入硬预算（必须）**：worker 单次输入 ≤ 8K tokens；法条检索只取 top-3（只给条文原文+ID+版本）；引用摘要每条约 ≤3 句；超出裁剪而非全塞——v4-flash 上下文有限，塞爆会截断降质
- 输出**强制结构化 schema**：
  ```json
  {
    "clauseId": "12.3",
    "clauseQuote": "原文摘录",
    "riskType": "违约金过高",
    "severity": "high|medium|low",
    "legalBasis": { "tier": "direct|indirect|none", "articleId": "", "version": "", "quote": "" },
    "evidence": "引用的 clauseFacts 依据（哪条事实/关键数值支撑判断）",
    "suggestion": "修改建议",
    "suggestionClauseText": "可直接替换的示范条款"
    // 注：worker 输出即 status=proposed 的 finding，status 由复核更新，不在此 schema 内
  }
  ```

**③ 复核 agent（对抗）** —— 两件事，分工明确：
| 职责 | 范围 |
|---|---|
| 判断风险是否真实成立（**过滤误报**） | **三档风险全查** |
| 核对法条引用（查证） | **只对"直接依据"档**（无具体条文可核对的硬核对只会催生新幻觉） |

**查证漏洞防堵（必须）**：高危风险的「无直接依据」档，复核**也必须确认"确无直接法条"**——防 worker 用"标无依据"逃避查证，让幻觉溜过。

**复核 → 打回 → 重证（一次迭代回环，agent 间双向协作）：**
复核判定某风险"不成立"时，**不直接丢弃**，而是带**驳回理由**打回原 worker：
- worker 认可 → 撤回该发现（**status=rejected，reason=worker 自撤**，不入报告）
- worker 有证据 → 补充论证并坚持（**status=disputed**），进报告并标**「有争议」**
- 只迭代一次，不无限循环（控成本）

这是 agent 之间真正的"讨价还价"，纳入消融实验（见 2.6），是面试的协作故事核心。

**④ 报告生成**
- 🔴🟡🟢 分级汇总 + 按严重度排序 + 风险分布
- 输出统一 JSON schema（一份结构三处消费：评测脚本 / 前端 / Word 导出）

**合同事实黑板（共享记忆）——贯穿 ①~④ 的公共数据层：**

所有 agent 通过一块共享的结构化"黑板"读写，而非点对点传消息。黑板是 LangGraph 的**结构化共享 state**，不是外部数据库：

```json
ContractFactsBlackboard:
  clauseFacts: [
    { clauseId, quote,
      keyNumbers: { amount, paymentDays, penaltyRatio, defectDays, warrantyMonths },
      definitions: { 术语 → clauseId },   // 定义条款映射
      references: [clauseId] }            // 交叉引用（源自 crossReferences）
  ]
  findings: [
    { id, worker, clauseId, riskType, severity,
      legalBasis: { tier, articleId, version, quote },
      evidence,                            // 引用的 clauseFacts 依据
      status: "proposed|upheld|rejected|disputed",
      rejectReason?,                       // 复核驳回理由
      reVerifyJustification? }             // 重证后坚持的理由
  ]
```

数据流：
- **① 抽取** 写入 `clauseFacts`——一次解析、全链复用（省 token + 保证信息一致）
- **② workers** 读取相关 `clauseFacts` 子集，写入 `findings`（status=proposed）
- **③ 复核** 读全部 findings，更新 status（upheld / rejected+理由 / disputed）；驳回的带理由打回原 worker 重证（撤回或更新 justification）
- **④ 报告** 读取 findings：有复核配置（B/C）时只读 status ∈ {upheld, disputed}；**无复核配置（消融 A 档）时所有 proposed 直接视为通过进报告**——否则 A 档报告为空，消融无法对比

实现约束：**用 LangGraph state 实现，禁止为黑板引入外部存储/数据库/缓存系统**；并发天然无冲突（clauseFacts 由抽取单一写入，findings 各 worker 只写自己命名的条目）。
**findings 去重规则（必须）**：同条款 + 同风险类型 + 同严重度的重复发现**一律合并（保留 evidence 更充分者），配置无关**；跨 worker 撞车：有复核（B/C）时由复核裁决保留其一，**无复核（A 档）时退化为按 evidence 更充分者合并**——否则 A 档重复计数虚增误报，消融基准失真。这是「事实卡片」的完整版，是跨 worker 组合风险与对抗回环的载体。

### 2.4 法条依据三档（worker 输出时必须自报标签）
| 档位 | 含义 | 允许的写法 |
|---|---|---|
| 直接依据 | 检索命中具体条文 | 引条文原文 + `articleId` + 版本 |
| 间接依据 | 只有原则性条款 | 标"间接依据：诚信/公平原则"，注明非直接条文 |
| 无直接依据 | 检索无强相关 | **必须显式写"提示性质，无明确法条依据"，禁止硬编** |

### 2.5 法条检索（RAG）
- 只对**精选 60~80 条手册**建向量库（嵌入成本可忽略）
- **嵌入模型：硅基流动 bge-m3**（DeepSeek 不提供 embedding API，这是硬依赖，勿用 DeepSeek 做向量化）
- **混合检索**：关键词（BM25/正则）+ 向量双路——法条是精确文本，纯向量会漏术语
- 检索结果带 `{法条ID, 版本, 条文号, 原文}`，一路带到报告 = "可溯源"
- 语料版本正确性需核验（与官方版对比），实现后做一次半小时核验

### 2.6 评测方案
**风险矩阵（先定义实体，再谈标注）：** 风险类型枚举（**唯一锚：worker 分类必须与矩阵一一对应，矩阵是分类的唯一来源，违约金等子类型归入违约责任类目**）+ 每类严重度 rubric（客观阈值示例：违约金比例>30%→high、付款期>90天且无违约金条款→medium）+ 判定 checklist。**植入缺陷的严重度、规则基线、worker 判定、人工复核全部以同一份风险矩阵为锚**——这是三源解耦的统一基准。

**金标准集三组（缺一不可）：**
| 组 | 内容 | 用途 |
|---|---|---|
| 植入缺陷组 | 官方购销示范文本为骨架 + 按风险矩阵植入缺陷（民法典买卖条款用于定矩阵/严重度 rubric，**非 worker 判定规则**，见三源解耦）；**植入记录 = 标准答案** | 算召回 |
| 干净合同组 | 示范文本原样/无争议条款 | **算误报率（误报密度，见 2.7 口径）** |
| 边界条款组 | 双方都有理的争议条款（违约金比例算高算低、管辖约定含糊），取材自真实裁判争议点 | 严重度校准、复核 agent 的"不武断"能力，**单独报，不进主指标** |

**三源解耦（防"背答案"，必须做）：** 植入缺陷的生成依据、worker 的判定规则、规则基线三者**必须解耦**：
- 植入缺陷生成时**混入"规则抓不到的变体"**——组合风险（付款期+质保金联动）、需跨条款推理的、表述含糊的
- **规则基线保持朴素**——只抓最简单直接的确定性模式（如"违约金比例>30%"），否则基线本身就是答案，worker 永远赢不了
- worker 判定规则**独立于植入依据**（worker 用通用法律审查 checklist，不是用植入记录反推）
- 植入记录的**严重度也必须按标注指南的同一套 rubric 判定**，保证标准答案内部自洽（否则严格口径的严重度永远对不上，召回被无谓拖垮）

**评测集规模（建议，写死防糊弄）：** 植入组 30 份（每份 1~3 个缺陷）、干净组 20 份、边界组 10~15 份。

**评测集拆分（防过拟合评测集，必须做）：** 三组合计切成 **dev（调参，~70%）** 与 **held-out test（最终汇报，~30%）**。prompt/规则/阈值只许在 dev 上调，**test 只在最终汇报时碰一次**。简历数字一律来自 held-out test。**test 不得与 dev 同模板族**——用不同模板/不同植入组合/不同种子生成，确保分布有实质差异，否则 held-out 名不副实。

**人工抽样复核协议：** 标注完成后随机抽 **~20% 合同**人工复核（植入记录与标注指南的一致性），在 README / 评测页注明"经人工抽样校验"。

**计分口径（必须先写进 score.py，再跑任何指标）：**
- **主指标 = 风险对级（严格）**：`(条款, 风险类型, 严重度)` 三元组与标准答案**逐字段完全一致**才计 1 次命中
  - 召回率 = 命中数 / 植入缺陷总数
  - 精确率 = 命中数 / 模型输出风险总数
- **部分分（辅助诊断）**：条款 0.4 + 类型 0.3 + 严重度 0.3。不进简历主数字，用于定位"哪个环节坏了"、消融归因、迭代先行指标
- **边界组指标（单独报，不按严格命中计分）**：① 是否将样本标记为"有争议"（识别模糊度）；② 严重度倾向与专家标注是否一致（校准）；③ 是否承认不确定性（防武断）
- **三组口径必须同时输出（防"复核降误报"说不清）：**
  1. **系统级**：以最终报告为准的严格口径指标（报告只含 upheld/disputed）
  2. **复核模块级**：复核滤掉了多少真误报、误杀了多少真阳性、打回重证后改判正确率——复核价值独立量化（**A 档无复核，此项不适用**）
  3. **规则基线**：同口径下的基线分数

**规则基线（免费第二大脑）：** 确定性代码规则（如 `违约金比例 > 30% → 高危`、`只约定仲裁未约定仲裁地 → 中危`），零共偏、零成本，作为"打赢基线"的对比对象。

**盲标交叉验证：** 硅基流动 Qwen（不同家族）盲标植入缺陷，防自证偏差（同家族共享盲区）。**用途限定**：盲标只用于评估金标准集的标注质量（抽查植入记录是否合理、是否漏植），**绝不用于修正 worker 输出**——它是标注质检，不是推理环节。

**消融实验（复核 agent 必须可证伪）：** 同一评测集上对比**三档复核配置**，比 精确率/召回率/F1/成本/延迟：
- **A. 无复核**
- **B. 复核直滤**（过滤误报，无打回）
- **C. 复核 + 打回重证**（一次迭代回环）

（`查证/自由模式`为复核 agent 内部可开关子项，默认查证模式，可单独跑。）**成本控制**：消融先在 dev 子集（约 10 份）快速跑通，正式消融再全量——三档全量跑 = 3 倍 token，别浪费。结果处理：精确↑召回↓看 F1 净效果；双↑锁死；没变化砍掉；召回↓误杀则重设计。**复核节点在 LangGraph 里必须可插拔，打回分支可开关。**

### 2.7 及格线（Definition of Done）
| 指标 | 及格线 |
|---|---|
| 植入缺陷组召回率 | **≥ 85%** |
| 干净合同组误报率 | **≤ 15%** |
| 赢过规则基线 | 召回或 F1 **≥ +10 个点** |
| 成本 / 延迟 | **< 1 元/份**，**< 60s/份** |

- 及格线**只压在"植入 + 干净"两组**；边界组单独报（诊断）
- **干净组误报率定义（防口径歧义）**：干净合同中被判定为风险的条款数 ÷ 干净合同条款总数——干净组无真阳性，精确率无法直接定义，故用"误报密度"衡量
- **及格线以 C 档（复核+打回，最终配置）为准**：A/B/C 三档都报数，但只有 C 档必须达标；成本/延迟同样按 C 档计
- 及格线定 85%/15% 而非 95%+：防"记住植入模式刷分"，留出消融提升的真实空间
- `score.py` 输出带 **通过/未通过** 标记（绿勾/红叉），面试页直接展示

### 2.8 交付形态
- **评测 CLI**（`score.py`）——简历数字来源，**优先级最高**
- **FastAPI 后端**：`/upload` `/report` `/health`，服务同一流水线
- **Vue 单页三视图**：
  1. **工作台**：拖拽上传 + 合同类型选择 + **3 份演出合同一键载入**（高危/干净/边界各一）+ **流水线阶段进度条**（条款抽取→N个worker并行→复核→报告，可视化多 agent 架构）
  2. **报告详情**：左**条款导航**（有风险的条款高亮红黄点）↔ 右**风险卡片流**（按严重度排序，可展开：原文/类型/严重度/法条三档+版本/建议/示范条款复制）；**双向联动**（点卡片高亮条款、点条款滚到风险）；顶部 🔴🟡🟢 统计卡 + 风险分布图；导出 JSON/Word
  3. **评测对比页**：金标准集指标表 + **消融对比表**（无复核 / 复核直滤 / 复核+打回 vs 规则基线），及格线通过/未通过标记
  4. **离线演示模式**：3 份演出合同预生成报告缓存，前端一键切换"在线/离线"，演示不依赖 API 可用性（防面试现场翻车）。**缓存报告必须由真实 pipeline 跑出后导出，严禁手工编写假报告**；仅对演出合同生效，新上传合同必须走在线
- **导出**：结构化 JSON + Word（`python-docx`）
- **不做**：登录、多用户、数据库、移动端适配

### 2.9 工程要求
- **可观测性**：每 agent 输入/输出/token/耗时 trace（LangSmith 或 OpenTelemetry）
- **失败处理**：LLM 超时重试、检索失败降级、**部分成功**（几个 worker 挂了报告照出）
- **输出校验/修复**：任何 agent 输出必须过严格 schema 校验；解析失败 → 带错误信息重试 1 次 → 仍失败标"解析失败"跳过该条，不让单条坏数据崩整条链
- **状态持久化**：任务状态可恢复（LangGraph Checkpointer）
- **缓存**：法条检索结果、相同条款分析复用；prompt 缓存
- **并发隔离（必须）**：FastAPI 每个请求**新建 graph/state 实例**——黑板是内存态，共享实例会串数据；评测/演示按单请求串行执行
- **模型路由**：抽取/报告用 v4-flash；workers 用 v4-flash 强 prompt；复核用 thinking 档
- **安全**：API key 只从环境变量读取、只存服务端；合同数据不落第三方存储

### 2.10 时间预算（硬约束）
```
评测与领域逻辑 ~60% ＞ 前端 ~25% ＞ 文档/演示 ~15%
```
UI 是"脸"，评测数字是"骨"。评测没跑出真实数字前，不许用前端美化掩盖。

---

## 3. 里程碑

| 阶段 | 周期 | 交付物 |
|---|---|---|
| **M1 MVP** | **3~4 周** | LangGraph 流水线（含合同事实黑板）+ 精选手册 RAG + `score.py`（三组口径计分/及格线/规则基线/dev-test 拆分）+ 三组金标准集（含人工抽查、三源解耦）+ **植入缺陷组召回 ≥85% 达标（C 档系统级口径，M1 即含复核节点并默认 C 档跑）**；**干净组误报≤15%、赢基线于 M2 以 C 档完成** |
| **M2 生产化** | +3~4 周 | 消融实验 + 盲标交叉验证 + FastAPI（每请求独立实例）+ Vue 三视图（含离线演示模式）+ 评测对比页 |
| **M3 加分项** | 有余力 | 双向联动打磨、Word 导出、Docker 部署、README+截图、边界组优化 |

---

## 4. 求职呈现要点（对实现者的约束）
- 简历数字只许用**严格口径**、只许**真实跑出**
- 一句话口径（面试背熟版）："我们把风险定义为『条款+类型+严重度』三元组，模型输出与标准答案逐字段完全一致才算命中"
- 差异化三件套：**对抗复核（消融验证）+ 法条可溯源（三档不硬编）+ 打赢规则基线（+10 个点）**
- 多 agent 协作故事（面试讲这套流程，而非只说"有多个 agent"）：**任务分解/并行分工 → 合同事实黑板（共享记忆/一次解析多处复用/跨 worker 组合风险）→ 对抗评审 → 复核打回重证（agent 间双向迭代）**
- 诚实声明：这是初筛助手，**人工终审**，不吹准确率，不构成法律意见
