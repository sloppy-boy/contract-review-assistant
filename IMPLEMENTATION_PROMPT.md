# 实现提示词 · 交付给执行 Agent

> 用法：把本文完整发给执行 agent（可同时附上 `PROJECT_SPEC.md`）。本文自包含，即使执行 agent 没有规格文件也能开工。
> 本文含**验收自查清单**，用于评估执行 agent 是否合格。

---

## 你的任务

从零实现一个**合同审查助手**：上传采购/销售合同 → 多 agent 流水线自动输出「风险条款清单 + 修改建议 + 可溯源法条依据」，并提供浏览器 Web 演示 + 真实评测数字。

这不是 demo，是一份**求职简历级项目**。及格线是：评测数字真实跑出、打赢规则基线、复核 agent 经消融验证、法条可溯源。禁止伪造任何指标。

**完整规格见 `PROJECT_SPEC.md`（同目录）。以下为关键决策摘要，两者冲突时以 SPEC 为准，且不得擅自改核心决策。**

## 技术栈（锁定，勿换）
- Python 3.11+，**LangGraph** 编排，FastAPI 后端
- **DeepSeek API**（v4-flash 主链路；复核用 thinking 档）；**硅基流动 SiliconFlow · Qwen3.5-122B-A10B** 做盲标第二模型（不同家族，防共偏；**勿用硅基流动上的 DeepSeek 系列，勿用 8B 弱模型**）；**法条嵌入用硅基流动 bge-m3**（DeepSeek 无 embedding API，硬依赖）
- Vue 3 + Element Plus（Vite）前端
- 评测 CLI：`score.py`
- API key 只从环境变量读（`DEEPSEEK_API_KEY`、`SILICONFLOW_API_KEY`），写入 `.env.example`，`.env` 入 `.gitignore`

## 流水线（LangGraph 图，顺序固定）
```
上传合同 → ①条款抽取 → ②风险识别workers(并行扇出) → ③复核agent → ④报告生成
```
- **① 抽取**：长文档分块/Map-Reduce + 保留条款编号与原文位置 + 输出 `crossReferences`（条款引用映射）；扫描件前置 OCR（PaddleOCR 或等效）；异常条款兜底；**分块后必须做全局关联步骤**（合并后再通读一次，生成全合同级 definitions/crossReferences——分块会切碎跨页引用）
- **② workers**：每类一个，**分类与风险矩阵一一对应（矩阵为分类唯一来源）**：付款/开票、验收交付、知识产权、数据合规、**违约责任（含违约金过高、违约救济等子类型，不单列）**、解除权、保密、竞业限制、管辖/仲裁、通知送达、不可抗力、税费、外包分包。**每个 worker 只收到"职责相关条款 + 被引用条款摘要(2~3句)"**，不得读全文。**输入硬预算 ≤8K tokens**（法条只取 top-3、引用摘要 ≤3 句/条，超出裁剪）。输出强制 schema：`{clauseId, clauseQuote, riskType, severity, legalBasis{tier,articleId,version,quote}, evidence, suggestion, suggestionClauseText}`（worker 输出即 status=proposed 的 finding，status 由复核更新）
- **输出校验/修复（全节点通用）**：任何 agent 输出必须过严格 schema 校验；解析失败 → 带错误信息重试 1 次 → 仍失败标"解析失败"跳过该条，**不崩整条链**
- **③ 复核**：两件事分工——过滤误报（**三档风险全查**）；核对法条引用（**只对"直接依据"档**）。**复核节点必须可插拔**（消融实验要用）。**查证漏洞防堵**：高危风险的"无直接依据"档也必须复核确认"确无直接法条"，防 worker 用标"无依据"逃避查证
- **复核→打回→重证**（agent 间双向协作，一次迭代）：复核判定"不成立"时**带驳回理由打回原 worker**——worker 认可则撤回（**status=rejected，reason=worker 自撤，不入报告**），有证据则坚持（**status=disputed**）并标「有争议」；只迭代一次，不无限循环
- **合同事实黑板（共享记忆）**：① 抽取把条款事实写入共享 state（`clauseFacts`：关键数值/定义/引用，**一次解析全链复用**）；② workers 读相关事实、写 `findings`（status=proposed）；③ 复核更新状态（upheld/rejected+理由/disputed）；④ 报告有复核（B/C）时只读 upheld/disputed、**无复核（消融 A 档）时 proposed 全视为通过**。**用 LangGraph state 实现，禁止引外部存储**。是跨 worker 组合风险 + 对抗回环的载体
- **④ 报告**：🔴🟡🟢 分级汇总 + 按严重度排序 + 统一 JSON schema（评测/前端/Word 三处共用）

## 法条依据三档（worker 必须自报）
- `direct`：检索命中具体条文，引原文+ID+版本
- `indirect`：原则性条款（诚信/公平），标注"间接依据"
- `none`：**必须显式写"提示性质，无明确法条依据"，禁止硬编**

## 法条库（RAG）
- 只建**精选买卖法条手册**（约 60~80 条）：民法典合同编买卖 595-647、违约 577-594、违约金 585、定金 586、民诉法管辖、反法商业秘密
- **混合检索**（关键词 + 向量双路），每条带 `{法条ID, 版本, 条文号, 原文}`
- **禁止引用 1999 旧合同法**；语料需与官方版核对

## 评测（必须最先跑起来的部分）
0. **先定义风险矩阵实体**：风险类型枚举（**worker 分类与之一一对应，是分类唯一来源**；违约金等归入违约责任类目）+ 每类 severity rubric（客观阈值示例）+ 判定 checklist——植入/基线/worker/复核**统一以此为锚**
1. **金标准集三组**：
   - 植入缺陷组：官方购销示范文本为骨架 + 按风险矩阵植入缺陷，**植入记录 = 标准答案**，另需标注指南
   - 干净合同组：示范文本原样（算误报）
   - 边界条款组：争议条款（违约金比例、管辖含糊），取材裁判争议点，**单独报、不进主指标**
   - 提供**生成脚本**（合成合同 + 植入记录），保证可复现
   - **规模建议**：植入组 30 份（每份 1~3 缺陷）、干净组 20、边界组 10~15
   - **三源解耦（防背答案）**：植入缺陷**混入规则抓不到的变体**（组合风险/跨条款推理/表述含糊）；规则基线保持朴素（只抓最直接确定性模式）；植入严重度按标注指南同一 rubric 定
   - **拆分 dev/test**：~70% 作 dev（调参用），~30% 作 held-out test（最终汇报用，**全程只碰一次**）；prompt/规则/阈值只在 dev 上调；**test 用不同模板/植入组合/种子，不得与 dev 同模板族**
   - **人工抽样复核**：标注后随机抽 ~20% 合同核对植入记录与标注指南一致性，README/评测页注明
2. **计分（先写计分，再跑指标）**：
   - 主指标=**风险对级严格口径**：`(条款,类型,严重度)` 逐字段全对才命中；召回=命中/植入总数；精确=命中/输出总数
   - 部分分辅助：条款0.4+类型0.3+严重度0.3（诊断用，不进简历）
   - **三组口径同时输出**：①系统级（最终报告）②复核模块级（滤掉真误报/误杀真阳性/打回改判正确率）③规则基线——复核价值独立量化
   - **边界组单独报**（不按严格命中）：标"有争议"识别率 / 严重度倾向一致性 / 是否承认不确定性
   - **findings 去重**：同条款+同类型+同严重度合并（保留 evidence 充分者），**配置无关**；跨 worker 撞车由复核裁决，**无复核（A 档）退化为按 evidence 充分者合并**——防重复计数虚增误报
3. **规则基线**：确定性规则 checker（代码写死），对比对象
4. **盲标交叉验证**：Qwen（硅基流动）盲标，防自证偏差；**用途=标注质量质检（抽查植入是否合理/漏植），不用于修正 worker 输出**
5. **消融实验**：三档复核配置 **A 无复核 / B 复核直滤 / C 复核+打回重证**，比精确率/召回率/F1/成本/延迟；查证/自由模式为复核内部可开关子项（默认查证）；**成本控制：先跑 dev 子集（约 10 份），正式消融再全量**

## 及格线（Definition of Done，score.py 输出通过/未通过标记）
| 指标 | 及格线 |
|---|---|
| 植入缺陷组召回率（C 档系统级） | ≥ 85% |
| 干净组误报率 | ≤ 15% |
| 赢规则基线 | 召回或 F1 ≥ +10 个点 |
| 成本 / 延迟 | < 1 元/份，< 60s/份 |

> 及格线以 **C 档（复核+打回，最终配置）** 为准：A/B/C 三档都报数，但只有 C 档必须达标；成本/延迟同样按 C 档计。
> **干净组误报率定义**：被判定为风险的条款数 ÷ 干净合同条款总数（干净组无真阳性，故用误报密度而非精确率）。

## 交付物清单
1. 项目代码（目录结构见下）+ `README.md`（架构图、使用说明、指标说明、截图占位）
2. `score.py`（计分 + 及格线 + 消融 + 规则基线 + 盲标）
3. 金标准集生成脚本 + 已生成的评测集 + 标注指南
4. FastAPI 后端（`/upload` `/report` `/health`）；**每个请求新建 graph/state 实例**（黑板是内存态，共享会串数据）
5. Vue 3 单页**三视图**：
   - 工作台：拖拽上传 + 合同类型 + **3 份演出合同一键载入**（高危/干净/边界）+ **流水线阶段进度条**（可视化多 agent 扇出）
   - 报告详情：左条款导航（风险高亮红黄点）↔ 右风险卡片（可展开：原文/类型/严重度/法条三档+版本/建议/示范条款复制）；**双向联动**；顶部统计卡 + 风险分布图；导出 JSON/Word
   - 评测对比页：指标表 + **消融对比表** + 及格线绿勾/红叉
   - **离线演示模式**：3 份演出合同预生成报告缓存，一键切换"在线/离线"，演示不依赖 API 可用性；**缓存必须是真实 pipeline 跑出后导出，严禁手工编报告**；仅对演出合同生效
6. 导出：Word（python-docx）+ 结构化 JSON
7. `Dockerfile`（可选，加分）+ `.env.example`

## 建议目录结构
```
contract-review-assistant/
├── app/
│   ├── graph.py            # LangGraph 流水线（复核可插拔）
│   ├── state.py            # 合同事实黑板 schema（clauseFacts / findings）
│   ├── nodes/              # extract / workers / reviewer / report
│   ├── legal/              # 法条手册数据 + 混合检索 + 风险矩阵
│   ├── api.py              # FastAPI
│   └── config.py
├── eval/
│   ├── score.py            # 计分+及格线+消融+规则基线+盲标质检
│   ├── make_dataset.py     # 金标准集生成（合成+植入记录）
│   ├── blind_label.py      # 盲标质检（Qwen 不同家族，评估标注质量）
│   ├── rule_baseline.py
│   └── dataset/
│       ├── dev/            # 调参集（~70%，只在它上面调）
│       ├── test/           # held-out 最终汇报集（~30%，只碰一次）
│       └── 标注指南.md
├── frontend/               # Vue3 + Element Plus 三视图
├── scripts/                # run.py / run-eval.py / run-server.py
├── .env.example
└── README.md
```

## 验收自查清单（对执行 agent 的要求，也是评估你的标准）
1. `python eval/make_dataset.py` 能生成可复现的三组评测集
2. `python eval/score.py` 能跑出真实指标，输出及格线通过/未通过，**无任何伪造数字**
3. LangGraph 流水线端到端跑通：一份合同 → 结构化报告 JSON
4. 复核节点开/关可切换，**打回重证分支可开关**，消融实验三档（A/B/C）数字可复现
5. FastAPI 三个路由可调通，返回报告 JSON
6. 前端三视图能跑，演示合同一键载入出报告
7. 法条引用全部带 ID/版本，**全库 grep 不到旧合同法**
8. README 完整，含架构图（mermaid）、运行步骤、指标表
9. **合同事实黑板生效**：同一事实被多个 worker 复用（可从 trace 验证）；findings 状态机（proposed→upheld/rejected/disputed）贯穿复核与打回重证
10. 评测集已拆 dev/test，**最终数字只在 held-out test 上跑**（test 全程只碰一次）
11. 演示合同支持**离线演示模式**（不依赖 API 可出完整报告）
12. 输出 schema 校验生效（坏 JSON 重试 1 次 → 标记跳过，不崩链）；**人工抽样复核 ~20% 已做并在 README 注明**
13. **三源解耦生效**：植入缺陷含规则抓不到的变体；规则基线保持朴素；test 集与 dev 不同模板族——评测数字不是"背答案"
14. **三组口径输出**：score.py 同时给系统级/复核模块级/规则基线；高危"无依据"档复核有确认；抽取含全局关联步骤；FastAPI 每请求独立实例；离线缓存为真实导出（非手工）
15. **风险矩阵为唯一锚**（植入/基线/worker/复核同一份）；法条嵌入用 bge-m3；worker 输入 ≤8K tokens 且有 evidence 字段；findings 去重生效且 **A 档退化规则正确**；及格线以 C 档为准

## 工作方式
- **分里程碑推进**：M1(MVP+评测达标) → M2(消融+FastAPI+前端) → M3(打磨加分)。每完成一个里程碑给出可运行的验证结果
- 先搭评测和流水线（约 60% 精力），再上前端（约 25%），文档垫底
- 任何偏离 SPEC 核心决策的改动，必须先说明理由再动
- 中途遇到无法解决的阻塞，停下来报告，不要默默绕路

## 禁止事项
- ❌ 伪造/估算评测指标
- ❌ 虚构法条引用
- ❌ 引用 1999 旧合同法
- ❌ 用前端美化掩盖"评测没跑出真实数字"这个事实
- ❌ API key 硬编码进代码或提交
- ❌ 为合同事实黑板引入外部数据库/缓存系统（用 LangGraph state 实现即可，不要过度设计）
